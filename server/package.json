{
  "name": "positivegrid-price-center-backend",
  "version": "0.1.0",
  "description": "PositiveGrid Price Center Backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=development npx webpack-dev-server --config webpack.js --color --progress --hot",
    "build:prod": "NODE_ENV=production npx webpack --config webpack.js --mode production",
    "build:staging": "NODE_ENV=staging npx webpack --config webpack.js --mode production",
    "build:dev": "NODE_ENV=development npx webpack --config webpack.js --mode development"
  },
  "dependencies": {
    "express": "^4.16.4",
    "mongoose": "^5.4.2"
  }
}
